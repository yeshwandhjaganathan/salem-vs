import ScrollToTop from '../ScrollToTop';

export default function ScrollToTopExample() {
  return (
    <div className="h-screen">
      <ScrollToTop />
      <p className="p-4">Scroll down to see the button appear (simulated as visible here)</p>
    </div>
  );
}
